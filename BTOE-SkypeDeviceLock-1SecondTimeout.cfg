<?xml version="1.0" encoding="utf-8"?>
<!--Description: When Device Lock is enabled and paired via BToE, the phone will lock 1 second after the phone detects that the PC is locked.-->
<PHONE_CONFIG>
  <ALL up.btoeDeviceLock.timeOut="1" />
</PHONE_CONFIG>